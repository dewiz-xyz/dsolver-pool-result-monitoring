apiVersion: 1

datasources:
  - name: DSolver-Prometheus
    type: prometheus
    uid: dsolver-prometheus
    orgId: 1
    access: proxy
    url: http://localhost:9000
    isDefault: false
    editable: true
    jsonData:
      timeInterval: 6s
